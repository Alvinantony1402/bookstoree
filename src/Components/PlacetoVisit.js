import { makeStyles } from '@material-ui/core';
import React from 'react'


const useStyles = makeStyles((theme) => ({
    root: {
       
    }

}));

function PlacetoVisit() {
  return (
    <div>PlacetoVisit</div>
  )
}

export default PlacetoVisit